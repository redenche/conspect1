ghbikj 5 xtkjdtr dct nfrb rhe;jr yt evbhftn

упоминая math/rand мы имеем в виду путь импортирования а не имя пакета.
путь импортирования это уникальная строка которая индефецирует пакет и используется уникальная дерективные import  после того как пакет будет импортиврован
к нему можно будет обращаться по имени
у некоторых пакетов которые использовались до сих пор путь импортиврования с именем пакеа несколько примеров:
1 столбец путь импортирования 2 столбец имя пакета
"fmt" - fmt
"log" - log
"strings" - strings
однако путь импротирования и имя пакета могут отличаться. многие пакеты go класифицируются по категориям-
например "сжатие" или "комплексные вычисления". по этой причине они группируются по префиксам пути импортирования например 
"archive/" или "math/" (их можно расматривать как аналог путей каталогов на диске)

go требует чтоб имя пакета было как- то связано с путём импортирования.но по соглашению последний сегмент пути импортирования так же используется в качестве имени пакета. таким образом для пути импортирования archive именем пакета так же будет archive
а для пути имопртирования "archive.zip" будет использоваться имя пакета .zip

rand.Intn()- функция которая возращает случайное число от 0 до заданого числа
так как эта функция считает в себе числа а не номер то она будет ыдавать следущее например
rand.Intn(100) - будет выдавать от 0 до 99 поэтому в нашем проекте мы после этого добавляем +1
мы можем ввести вместо 100 101
псевдо случайность
иногда при функциии случайности мы получаем одинаовые числа иногда подряд потому что наш генератор слуайных чисел следует по определенному алгоритму и его можно предугадать
функцией randSeed мы инициализируем генератор случайных чисел и предостовляем значение которое будет вступать для генерации дргух случйных чисел но если передавать одно и тоже значение то числа будут одинаковыми и мы вернемся с тог с чего начали
крч randSeed ест числа напимер дату или секунды или другую фигню и усложняет наши рандомные числа
.Unix()- считает секунды от 01.01.1970 до какого либо времени например у нас эта функция считает до нашего времени




















































































































































































































































































































































































































































                                                        




























